"use strict";

/**
 * Configuración del cliente MexClub.
 *
 * API_BASE_URL: URL completa del servidor API.
 *   - En producción: "https://api.tudominio.com/api"
 *   - Si la app se sirve desde el mismo servidor que la API: "/api"
 *
 * Cambiar este valor es lo ÚNICO necesario para conectar la app a otro servidor.
 */
var MEXCLUB_CONFIG = {
    API_BASE_URL: "/api",
    APP_NAME: "MexClub"
};
